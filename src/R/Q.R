




.Q$solve <- function(equations){
        #C <- 
        rref(
                matrix(
                        c(c(1,-2),c(0,-2),c(3,6)),
                        nrow=nrow(equations)
                        )
        )
}